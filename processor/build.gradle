apply plugin: 'java'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile project(path: ':routerannotation')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
